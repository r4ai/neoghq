use anyhow::Result;

pub fn execute(branch: String) -> Result<()> {
    println!("Creating worktree for branch: {branch}");
    println!("worktree create functionality not yet implemented");
    Ok(())
}
